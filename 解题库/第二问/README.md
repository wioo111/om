# 第二问正式入口

从仓库根目录运行：

```powershell
python 解题库/第二问/run.py --s1 0 0 --bearing 0
python 解题库/第二问/run.py --s1 2100 400 --bearing 190 --side -1
```

唯一策略实现为 `../第一二问/src/q12/policy.py`。理论最优点和带向内保护的执行点分别输出；未截断状态的最优性与全部有效首次观测的策略级极小极大保证，不意味着每个截断状态逐状态最优。

旧 v3/v4/v5 及其代码、报告和图片**不再作为正式计算和论文结果源**；Q1/Q2 旧 v5 目录已移除。
