# Q1 Q2 冻结说明

- 冻结日期：2026-09-13
- 正式代码：`解题库/第一二问/src/q12/geometry.py`、`posterior.py`、`policy.py`、`proof.py`、`interval.py`
- 唯一论文源：`解题库/第一二问/paper/正文模板.md`
- 最终理论最优点（相对首次示向度坐标）：`(843.034753748736, ±545.528422439214) m`
- 最坏直径：`110.969318572838... m`
- `run.py prove`：PASS；整数区间证书实际重算通过，63 个非活动示向度区间叶区间连续覆盖。
- `run.py test`：PASS；164 项通过，0 失败、0 错误、0 跳过。
- `run.py experiments`：实际完成 1000 个 Q1 区域和 6000 次 Q2 配对评估。
- `run.py figures`：PASS；13 张正式图均生成 PNG、PDF、SVG。
- `run.py paper`：PASS；由唯一正文模板重新生成 Markdown、LaTeX、DOCX、PDF，并同步到 `解题库/完整论文/`。
- 最终 `run.py verify`：PASS；重新执行证明证书、测试统计、实验记账、图片清单、论文同步和 DOCX 原生公式检查。

## 证明适用范围

1. 对未被目标区域 `Ω` 截断的标准首次扇形，两个站位是完整连续接收安全域上的精确全局最优点。
2. 对任意有效首次观测，平移旋转后的统一策略达到策略级极小极大全局最优保证。
3. 不声称每一个被 `Ω` 截断的具体状态都逐状态达到该状态自己的最优值；截断状态的四圆交集是通用充分域，不必是该状态的完整安全域。

除发现可复现数学或代码错误外，不再修改 Q1/Q2 模型。
