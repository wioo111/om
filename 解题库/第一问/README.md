# 第一问正式入口

从仓库根目录运行：

```powershell
python 解题库/第一问/run.py --input 解题库/第一二问/examples/q1_observations.json
```

唯一几何实现为 `../第一二问/src/q12/geometry.py`，正文由 `../第一二问/paper/正文模板.md` 生成。旧 v3/v4/v5、根部代码与旧图片**不再作为正式计算和论文结果源**；Q1/Q2 旧 v5 目录已移除。第三、四问所需的独立内联兼容副本保持原样。
