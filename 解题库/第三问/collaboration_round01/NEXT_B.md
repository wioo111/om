# B 下一轮任务：区分参数收益，再减少低收益检测

继续只修改 `collab/measure_candidate.py`，保留 `MeasureMixin` 和协作初始化接口；公共算法、模拟器与结束条件保持冻结。以 `ROUND01_REPORT.md` 的本机统一结果为准。

B01 同时改变前进距离、侧移距离、试清半径三个参数。原 21 局开发报告中，清除失败从 65 次降到 32 次，但每源移动只减少约 0.95 s，检测反而增加约 0.48 s，总体只改善约 0.73 s/源。需要区分哪些变化有效、哪些互相抵消。

下一轮先用同一开发集做少量拆分比较：

1. 只将 `probe_radius` 改为 50，其他参数保持 v8。
2. 只将 `first_hop/lateral` 改为 200/100，试清半径保持 v8。
3. 根据前两项的移动、检测和失败清除成本，选一个有依据的下一版；不做大范围盲目参数网格。

主要目标仍是减少低收益扫描和补测：依据已有记录、当前定位区域和新位置能增加的信息选择动作。不要在同一点重复检测平均固定误差；不能读取源真值、实际 N 或场景种子，也不能省略全清确认。

每轮最多 3–4 个候选，只交一个胜者及对应结果、轨迹。报告必须拆出移动、检测、切频和失败清除的变化，不能仅报告失败次数降低。

本轮验收场景公布后可转为开发回归；下一轮独立验收由 C 重新生成。禁止视觉复查、反复审计。
