# AI is optional. Copy to .env locally when ready; never commit the real .env.
PAPER_AI_PROVIDER=openai-compatible
# OpenAI-compatible base should include /v1; Ollama base normally ends at :11434.
PAPER_AI_BASE_URL=
PAPER_AI_MODEL=
PAPER_AI_API_KEY=
