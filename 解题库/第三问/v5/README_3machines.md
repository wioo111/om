# 第三问三机协作入口（v8）

原三机说明对应 v5。当前请使用 [v8 三机协作说明](collab/README.md)。

- A：路线与覆盖，只改 `collab/route_candidate.py`。
- B：检测与定位，只改 `collab/measure_candidate.py`。
- C：统一验收、合并、官方演练。

基线与场景版本统一，结果按相同场景成对比较。基线自动缓存，不重复跑完整旧基线。详细命令与各机开工指令均在 `collab` 目录。
