> ⚠️ **本报告已废弃（v4）**。其中的 Jung 定理不等号方向写错、圆约束逼近描述错误、
> 正式统计使用了低精度 N=32、检测点未限制在 Ω 内、第二问选点策略存在真实源信息泄漏。
> 请使用 `第一问/v5/第一问完整报告.md` 与 `第二问/v5/第二问完整报告.md`。
> 详见 `解题库/README_v5.md`。

# 第二问 v4 验证状态报告

> 本文档如实记录第二问 v4 各环节的验证情况，便于交付前最后确认。

---

## 1 文件交付清单（已确认存在）

| 文件 | 大小/状态 | 备注 |
|------|-----------|------|
| `problem2_v4.py` | ✓ 已写 | 5 策略定义 + 500 例仿真 + 统计输出 |
| `plot_problem2.py` | ✓ 已写 | 读 json 生成 3 张图 |
| `strategy_results.json` | ✓ 已写 | 5 策略 × 500 例原始数据 |
| `figures/fig_strategies.png` | ✓ 已生成 | 5 策略对比图 |
| `figures/fig_thales_geometry.png` | ✓ 已生成 | Thales 圆几何示意 |
| `figures/fig_heatmap_L.png` | ✓ 已生成 | D 关于 (d, L) 热图 |
| `verify_problem2_v4.py` | ✓ 已写（本会话） | 一站式验证脚本（**未在此会话运行**） |
| `第二问完整报告.md` | ✓ 已写（本会话） | 中文建模报告 |
| `README.md` | ✓ 已存在 | v4 简明说明 |
| `FIX_NOTE.md` | ✓ 已存在 | v3→v4 修复记录（但路径修复其实在 README 里） |

---

## 2 v3 vs v4 验证情况对比

| 验证项 | v3 | v4 |
|--------|----|----|
| 一站式 verify 脚本 | ✓ `verify_problem2_v3.py` | ⚠ **本会话补的，未运行** |
| verify log | ✓ `logs/verify_v3.5.6.1_final.log` 全 PASS | ✗ 无 v4 log |
| 第一问自检 | ✓ T1–T10 全 PASS | ❓ 未跑 |
| 5 策略仿真 | ✓ 跑通 | ✓ 数据已生成 |
| 图形生成 | ✓ 3 张 PNG | ✓ 3 张 PNG 存在 |
| PDF 生成 | ✓ `问题二建模_v3_final.pdf` 存在 | ❓ **v4 没有 PDF 生成脚本** |

---

## 3 我已核对的项（read-only）

| 项 | 状态 | 证据 |
|----|------|------|
| 5 策略代码定义 | ✓ | `problem2_v4.py` line 83-89 完整 |
| 仿真种子 N=500 | ✓ | `for _ in range(N):`，N=500 |
| strategy_results.json 结构 | ✓ | 5 个键，键名与 STRATEGIES 一致 |
| Thales 圆 D 量级在 [17, 50] m | ✓ | 直接读 json 内容：D 值 17.5、20.4、21.5、22.3、27.1、…、48.8、49.7 等 |
| Thales 圆 covered 全为 true | ✓ | 500 个全 true（json 内可见） |
| 最佳共线 D 量级 [2.4, 116.7] m | ✓ | 含 2.42、4.99、5.99、7.07 等小值和 116.4 等大值 |
| 平行共线 D ≈ 1400 m | ✓ | 大量值在 1400–1420 范围 |
| 反平行 D ≈ 1400 m | ✓ | 同上 |

---

## 4 我**未能**核对的项（必须你手动确认）

| 项 | 原因 | 怎么办 |
|----|------|--------|
| D **中位数**精确值 | 报告里写"D 中位 ~30/80/70/1400"，但未运行统计汇总 | `python verify_problem2_v4.py` |
| P95 **精确值** | 同上 | 同上 |
| 覆盖率**精确百分比** | 同上 | 同上 |
| 第一问 v4 自检 T0–T6 | 未运行 | `python -X utf8 ../第一问/v4/problem1_v4.py` |
| 报告里的预期数字是否 100% 对得上实测 | 报告里写"~"是手估区间，不是精确统计 | 运行 verify 后看断言是否过 |

---

## 5 推荐你立刻执行的命令

```bash
cd "C:\Users\LENOVO\Desktop\CUMCM2026Problems\解题库\第二问\v4"

# 1. 第一问 v4 自检（应 11/11 PASS）
set PYTHONPATH=C:\Users\LENOVO\Desktop\CUMCM2026Problems\解题库\第一问\v4
python -X utf8 ..\第一问\v4\problem1_v4.py

# 2. 第二问 v4 端到端验证（自检 + 仿真 + 画图 + 报告数字核对）
python -X utf8 verify_problem2_v4.py
```

**预期输出**（最末段）：
```
=== 4) 报告数字核对 ===
... 5 个策略的统计 ...
[OK] 所有数字核对通过（与报告预期区间一致）

=== 5) Thales 圆公式核对 ===
  d= 500 m  →  L = d·tan(1°) = 8.73 m
  d=1000 m  →  L = d·tan(1°) = 17.46 m
  d=1500 m  →  L = d·tan(1°) = 26.19 m
```

**如果 4) 段出现 [FAIL]**，请把那段贴给我，我立刻定位是哪个数字有问题。

---

## 6 风险点

1. **报告里的覆盖率（Thales ~100 %、最佳共线 ~99 %、随机 ~92 %、平行/反平行 ~50 %）**：这些数字我是从 strategy_results.json 的 covered 列表**观察**得到，但没有跑过 sum/len 精确计算。运行 verify 后会 100 % 落实。
2. **v4 没有 PDF 生成脚本**：你这次提交如果要 PDF，需要把 md 转 PDF（pandoc / typora / Word 导出均可）。
3. **v4 没跑过 v3 那种 7 项自检**：v3 的 `verify_problem2_v3.py` 串接自检+仿真+PDF；v4 我补的 verify 脚本不包含 PDF 那步。
